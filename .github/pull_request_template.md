## WHAT
<!-- High-level overview of what this PR does. Keep it brief — 1-3 bullet points.
     Don't describe every file change; focus on the user-visible or architectural change.
     Technical details belong as review comments on the diff itself.

     Example:
     - Add a `window` column to the picker showing the tmux window name
     - Make columns configurable via `@claude-picker-columns` tmux option
-->


## WHY
<!-- Why is this change needed? What problem does it solve or what goal does it serve?

     Example:
     Some users assign semantic names to tmux windows rather than sessions.
     Showing the window name makes it easier to identify Claude instances.
-->

